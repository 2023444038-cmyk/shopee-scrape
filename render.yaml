services:
  - type: web
    name: shopee-absa
    runtime: docker
    envVars:
      - key: MODEL_GDRIVE_ID
        sync: false
      - key: TOKENIZER_GDRIVE_ID
        sync: false
      - key: SECRET_KEY
        generateValue: true
